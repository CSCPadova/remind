app-debug.apk e' per debug
app-release.apk e' il file che si usa per distribuire l'app

keystore.jks e' il keystore usato per firmare l'apk (usando Android Studio)

la password per il keystore e' "magnetophone" ma se ne possono creare di nuovi (sempre sfruttando Android Studio per fare le cose facilmente)
